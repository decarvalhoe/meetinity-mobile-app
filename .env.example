# HTTP API configuration
VITE_API_BASE_URL=http://localhost:8080
VITE_API_TIMEOUT=30000
VITE_TOKEN_REFRESH_THRESHOLD=120000

# Optional: override the realtime streaming endpoint exposed by the backend
# VITE_REALTIME_URL=http://localhost:8080/realtime

# Optional: expose the public callback URL registered with your OAuth providers
# VITE_OAUTH_REDIRECT_URI=http://localhost:5173/auth/callback

# Optional feature flags
# VITE_ENABLE_ANALYTICS=false
# Offline resilience is always enabled and does not require a flag.
